import { css } from 'styled-components';

const styles = css`
    footer {
        padding: 1em;
    }
    footer a {
        font-weight: bold;
        color: black;
        text-decoration: none;
        cursor:pointer;
    }
 
`;

export default styles;